download this project in a linux system or subsystem (wsl typically)

open a terminal

reach the root of the project via the cd command:

	cd 'your root'

first you have to build the docker image of the flask webapp :

	docker-compose up

once the docker-compose command is done, you can visit the web application at the following URL :

	http://localhost:5000/